@react.component
let make = () => {
    let (userProfiles, setUserProfiles) = React.useState(() => "")

    <div id="searchSuggestions">
        <input type_="text" className="searchBox" />
    </div>
}

